    @app.route('/threatanalysis/<status>')
    def APICall(status):
        try:
            for x in test["responsedata1"]:
                exec(open("../app/virusapi/test.py").read())
            return render_template('test.html',test=test)
        except:
        @app.errorhandler(werkzeug.execeptions.HTTPException)
        def error():
            redirect ("/404") 